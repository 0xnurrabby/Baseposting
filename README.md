# BasePosting — Base + Farcaster Mini App

Hard-coded domain: https://baseposting.online/

## Verify (must pass Mini App chrome)
- https://baseposting.online/.well-known/farcaster.json
- `<meta name="fc:miniapp">` and `<meta name="fc:frame">` with `launch_frame`
- `sdk.actions.ready()` called inside Mini App

## Env
Copy `.env.example` values into your deployment environment.
